# Repository upload note

Do not upload generated website assets to the repository until the final source ZIP and live URL are available. The existing documentation already gives the website builder everything it needs. Once the final site is delivered, add the frontend source in a dedicated directory (for example `frontend/`) and update the main README and submission evidence with the live URL and integration status.
