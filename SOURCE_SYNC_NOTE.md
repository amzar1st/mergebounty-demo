# Source synchronization note

The canonical main-branch contract source is being synchronized to the final Studionet-tested v3 implementation containing the zero-value `probe_evidence` preflight and evidence fetching that depends only on response body bytes rather than unsupported HTTP status fields. The immutable reviewed developer commit remains unchanged.
